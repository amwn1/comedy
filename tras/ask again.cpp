#include <iostream>
using namespace std;

   void display(vector<int> &v){
   for(int i=0;i<v.size();i++)
   {
    cout<<v[i]<<" ";
   } 
    cout<<endl;
}
int main()
vector<int> vec1;
for (int i = 0; i<4: i++)
{
    cout<<"Enter elements to add in the vector:";
  cin>>elemnt;
    vec1.pushback(element);
}
display(vec1)
return 0;
}

    